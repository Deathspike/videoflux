{
  "version": "0.2.0",
  "configurations": [
    {
      "args": ["bin/videoflux"],
      "name": "Launch Program",
      "outFiles": ["${workspaceFolder}/dist/**/*"],
      "outputCapture": "std",
      "preLaunchTask": "TypeScript",
      "request": "launch",
      "type": "node"
    }
  ]
}
